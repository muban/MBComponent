<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demo.mb">

    <application
        android:name=".AppApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MaterialTheme">
        <activity android:name=".view.ui.activity.HiltActivity" />
        <activity android:name=".view.ui.activity.MaterialDemoSecondActivity" />
        <activity android:name=".view.ui.activity.MaterialDemoActivity" />
        <activity android:name=".view.ui.activity.ApkActivity" />
        <activity android:name=".view.ui.activity.ComposeActivity" />
        <activity android:name=".view.ui.activity.MotionActivity" />
        <activity android:name=".view.ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>